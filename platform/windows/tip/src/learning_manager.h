#pragma once

namespace astelio::tip {

// D-05: a window that lists the history of chosen words, to delete some of them or all. Opened from the menu of
// the input mode button in the taskbar. One window per process; opening it again brings it to the front.
void ShowLearningManager();

} // namespace astelio::tip
